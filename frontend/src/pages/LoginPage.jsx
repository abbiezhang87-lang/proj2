import React from 'react';

// Spec §For Employees / §For HR — same form; server detects role.
// TODO: react-hook-form + authApi.login + redirect to PersonalInfo / Home.
export default function LoginPage() {
  return <div>TODO: Login page</div>;
}
