import React from 'react';

// Reusable file input; wraps MUI Button + hidden <input type="file">.
// TODO: validate MIME + size, surface progress.
export default function DocumentUploader(/* { onUpload, accept } */) {
  return <div>TODO: DocumentUploader</div>;
}
