import React from 'react';

// Spec requires in-browser preview + download per document.
// TODO: render PDFs in <iframe>, images in <img>, others fall back to download.
export default function DocumentPreview(/* { document } */) {
  return <div>TODO: DocumentPreview</div>;
}
