import React from 'react';

// Fields: firstName*, lastName*, middleName, preferredName, profilePicture.
// TODO: react-hook-form Controller'd MUI inputs; supports read + edit modes.
export default function NameSection(/* { control, readOnly } */) {
  return <section>TODO: NameSection</section>;
}
