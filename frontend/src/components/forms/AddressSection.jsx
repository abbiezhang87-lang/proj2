import React from 'react';

// Fields: building/apt #, street, city, state, zip (all required on onboarding).
export default function AddressSection(/* { control, readOnly } */) {
  return <section>TODO: AddressSection</section>;
}
