import React from 'react';

// Work authorization: visa title + start/end dates.
// Onboarding branches on "citizen/PR?" → Green Card/Citizen vs. visa type.
export default function EmploymentSection(/* { control, readOnly } */) {
  return <section>TODO: EmploymentSection</section>;
}
