import React from 'react';

// Fields: cellPhone*, workPhone, email (readonly — pinned to registration).
export default function ContactSection(/* { control, readOnly } */) {
  return <section>TODO: ContactSection</section>;
}
