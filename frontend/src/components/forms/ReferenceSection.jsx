import React from 'react';

// Single referrer (firstName*, lastName*, middleName, phone, email, relationship*).
export default function ReferenceSection(/* { control, readOnly } */) {
  return <section>TODO: ReferenceSection</section>;
}
