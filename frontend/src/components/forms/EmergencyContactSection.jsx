import React from 'react';

// One or more emergency contacts (firstName*, lastName*, middleName,
// phone, email, relationship*).
// TODO: use RHF useFieldArray for add/remove.
export default function EmergencyContactSection(/* { control, readOnly } */) {
  return <section>TODO: EmergencyContactSection</section>;
}
