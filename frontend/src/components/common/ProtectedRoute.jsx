import React from 'react';

// TODO: read auth state; if no user, redirect to /login.
// Optional `role` prop to restrict to HR-only pages.
export default function ProtectedRoute({ children /* , role */ }) {
  return children;
}
